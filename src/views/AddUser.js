import React from 'react';
import PropTypes from 'prop-types';

const AddUser = props => {
    return (
        <div>
            
        </div>
    );
};

AddUser.propTypes = {
    
};

export default AddUser;